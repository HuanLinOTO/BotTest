name: Hello World

on:
  push:
    branches:
      - main  # 只有当推送到 main 分支时才会触发

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: Print Hello World
        run: echo "Hello, World!"
